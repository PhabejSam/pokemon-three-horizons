# Playtest 5 repair plan

**Goal:** Repair outfit running, make the rival approach and collect his partner, and investigate Chikorita's follower appearance.

**Execution:** Native inline implementation using executing-plans; review the complete change at the end.

**Architecture:** Keep existing assets, save layout, starter selection and battle data. Correct per-outfit frame references. Use stage-gated on-frame scripts after door transitions and collision-checked movements in the existing maps.

**Spec:** The user's latest playtest report: Gold/Kris/Silver running glitches; visible rival partner pickup; automatic home greeting and first challenge; Chikorita's head appears missing while Bayleef looks correct.

**Tech stack:** Emerald expansion C, event scripts, Python host/ELF checks, GitHub Actions ARM build.

- [x] Add an ELF regression that follows animation/image pointers for all seven outfits. Confirm failure on playtest 4; fix frame tables and Red/Leaf direction order.
- [x] Add host coverage for automatic scene dispatch and lab movement collisions. Implement once-only greeting/challenge and regional Poké Ball pickup without changing rival party selection.
- [x] Compare Chikorita source, palette, frame bounds and alternative native asset; change only a demonstrated fault. Document any remaining art limitation.
- [x] Run host checks, review complete diff, build ROM and engine tests, verify compiled pointers, package a separate playtest-5 ROM and evidence. Preserve previous saves and ROMs.

## Decisions and evidence

- The old `2169b14` ELF fails the new checker on Gold/Kris/Silver asset addresses
  and Red/Leaf direction order. The `8155f62` ROM build passes all 112 frame reads.
- Twelve host checks pass. The independent whole-change review found no blocking
  issue; doorway task completion precedes the on-frame scene check.
- Ruling: keep Chikorita's existing artwork. Its six frames and palettes are intact;
  the screenshot matches its shiny rear view, and the alternate classic HnS sheet
  has the same silhouette. A redesign would be a separate art choice, not a proven
  rendering fix. Silver retains walking poses at running speed.
- Documentation indexing initially failed because these two new notes were absent
  from SUMMARY.md. Added both entries and reran the same checker successfully.
- Workflow 35914135874 passed: 26 project tests, four save tests, 12 host checks,
  all 112 running-frame reads, first-battle negative/positive regression, and
  trainer build-mode regeneration. Downloaded ROM and ZIP were verified locally.
